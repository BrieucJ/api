# ============================================
# Backend API Environment Variables
# ============================================
# Copy this file to .env.dev (or .env for legacy support)
# Required variables must be set, optional variables have defaults

# --------------------------------------------
# Application Configuration
# --------------------------------------------

# Environment: development, staging, production, or test
# Default: development
NODE_ENV=development

# Server port
# Default: 3000
PORT=3000

# Log level: fatal, error, warn, info, debug, trace, or silent
# Required: Yes
LOG_LEVEL=info

# --------------------------------------------
# Database Configuration
# --------------------------------------------

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Required: Yes
# Example: postgresql://postgres:password@localhost:5432/api_db
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# --------------------------------------------
# Authentication Configuration
# --------------------------------------------

# JWT secret key for token signing
# Must be at least 32 characters long
# Required: Yes
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# JWT token expiration time
# Default: 24h
# Examples: 1h, 24h, 7d, 30d
JWT_EXPIRES_IN=24h

# --------------------------------------------
# AWS Configuration (Required for production/staging)
# --------------------------------------------

# AWS region (e.g., us-east-1, eu-west-3)
# Required: Yes (in production/staging)
# Optional: No (in development)
REGION=eu-west-3

# SQS queue URL for job processing
# Required: Yes (in production/staging)
# Optional: No (in development - uses local worker)
# Example: https://sqs.eu-west-3.amazonaws.com/123456789012/worker-queue
SQS_QUEUE_URL=

# --------------------------------------------
# Service URLs (Optional)
# --------------------------------------------

# Worker HTTP endpoint (for local development)
# Only needed when running worker locally
# Default: Not set (uses SQS in production)
# Example: http://localhost:8081
WORKER_URL=http://localhost:8081

# API URL (for self-referencing)
# Optional: No
# Example: http://localhost:3000
API_URL=http://localhost:3000
