// import { hc } from "hono/client";
// import type { AppType } from "@backend/api";

// export const client = (baseUrl: string) => {
//   return hc<AppType>(baseUrl, {
//     init: { credentials: "include" },
//   });
// };
export type { AppType } from "@backend/api";
